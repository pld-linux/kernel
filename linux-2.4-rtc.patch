diff -Nur linux-2.4.22/arch/cris/drivers/ds1302.c linux-2.4.22.rtc_fix/arch/cris/drivers/ds1302.c
--- linux-2.4.22/arch/cris/drivers/ds1302.c	2003-08-25 13:44:39.000000000 +0200
+++ linux-2.4.22.rtc_fix/arch/cris/drivers/ds1302.c	2004-02-14 04:51:53.000000000 +0100
@@ -346,6 +346,7 @@
 		{
 			struct rtc_time rtc_tm;
 						
+			memset(&rtc_tm, 0, sizeof (struct rtc_time));
 			get_rtc_time(&rtc_tm);						
 			if (copy_to_user((struct rtc_time*)arg, &rtc_tm, sizeof(struct rtc_time)))
 				return -EFAULT;	
diff -Nur linux-2.4.22/arch/cris/drivers/pcf8563.c linux-2.4.22.rtc_fix/arch/cris/drivers/pcf8563.c
--- linux-2.4.22/arch/cris/drivers/pcf8563.c	2003-08-25 13:44:39.000000000 +0200
+++ linux-2.4.22.rtc_fix/arch/cris/drivers/pcf8563.c	2004-02-14 04:55:18.000000000 +0100
@@ -220,6 +220,7 @@
 		{
 			struct rtc_time tm;
 
+			memset(&tm, 0, sizeof (struct rtc_time));
 			get_rtc_time(&tm);
 
 			if (copy_to_user((struct rtc_time *) arg, &tm, sizeof tm)) {
diff -Nur linux-2.4.22/arch/m68k/bvme6000/rtc.c linux-2.4.22.rtc_fix/arch/m68k/bvme6000/rtc.c
--- linux-2.4.22/arch/m68k/bvme6000/rtc.c	2003-06-13 16:51:31.000000000 +0200
+++ linux-2.4.22.rtc_fix/arch/m68k/bvme6000/rtc.c	2004-02-14 04:56:43.000000000 +0100
@@ -54,6 +54,7 @@
 		/* Ensure clock and real-time-mode-register are accessible */
 		msr = rtc->msr & 0xc0;
 		rtc->msr = 0x40;
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		do {
 			wtime.tm_sec =  BCD2BIN(rtc->bcd_sec);
 			wtime.tm_min =  BCD2BIN(rtc->bcd_min);
diff -Nur linux-2.4.22/arch/m68k/mvme16x/rtc.c linux-2.4.22.rtc_fix/arch/m68k/mvme16x/rtc.c
--- linux-2.4.22/arch/m68k/mvme16x/rtc.c	2003-06-13 16:51:31.000000000 +0200
+++ linux-2.4.22.rtc_fix/arch/m68k/mvme16x/rtc.c	2004-02-14 04:58:03.000000000 +0100
@@ -52,6 +52,7 @@
 		cli();
 		/* Ensure clock and real-time-mode-register are accessible */
 		rtc->ctrl = RTC_READ;
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		wtime.tm_sec =  BCD2BIN(rtc->bcd_sec);
 		wtime.tm_min =  BCD2BIN(rtc->bcd_min);
 		wtime.tm_hour = BCD2BIN(rtc->bcd_hr);
diff -Nur linux-2.4.22/arch/ppc64/kernel/rtc.c linux-2.4.22.rtc_fix/arch/ppc64/kernel/rtc.c
--- linux-2.4.22/arch/ppc64/kernel/rtc.c	2003-06-13 16:51:32.000000000 +0200
+++ linux-2.4.22.rtc_fix/arch/ppc64/kernel/rtc.c	2004-02-14 04:59:03.000000000 +0100
@@ -96,6 +96,7 @@
 	switch (cmd) {
 	case RTC_RD_TIME:	/* Read the time/date from RTC	*/
 	{
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		ppc_md.get_rtc_time(&wtime);
 		break;
 	}
diff -Nur linux-2.4.22/drivers/acorn/char/i2c.c linux-2.4.22.rtc_fix/drivers/acorn/char/i2c.c
--- linux-2.4.22/drivers/acorn/char/i2c.c	2003-08-25 13:44:40.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/acorn/char/i2c.c	2004-02-14 04:59:59.000000000 +0100
@@ -166,6 +166,7 @@
 		break;
 
 	case RTC_RD_TIME:
+		memset(&rtctm, 0, sizeof(struct rtc_time));
 		get_rtc_time(&rtc_raw, &year);
 		rtctm.tm_sec  = rtc_raw.secs;
 		rtctm.tm_min  = rtc_raw.mins;
diff -Nur linux-2.4.22/drivers/char/ds1286.c linux-2.4.22.rtc_fix/drivers/char/ds1286.c
--- linux-2.4.22/drivers/char/ds1286.c	2003-08-25 13:44:41.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/char/ds1286.c	2004-02-14 05:02:00.000000000 +0100
@@ -174,6 +174,7 @@
 		 * tm_min, and tm_sec values are filled in.
 		 */
 
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		ds1286_get_alm_time(&wtime);
 		break;
 	}
@@ -216,6 +217,7 @@
 	}
 	case RTC_RD_TIME:	/* Read the time/date from RTC	*/
 	{
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		ds1286_get_time(&wtime);
 		break;
 	}
diff -Nur linux-2.4.22/drivers/char/efirtc.c linux-2.4.22.rtc_fix/drivers/char/efirtc.c
--- linux-2.4.22/drivers/char/efirtc.c	2003-06-13 16:51:32.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/char/efirtc.c	2004-02-14 05:03:00.000000000 +0100
@@ -118,6 +118,7 @@
 static void
 convert_from_efi_time(efi_time_t *eft, struct rtc_time *wtime)
 {
+	memset(wtime, 0, sizeof(struct rtc_time));
 	wtime->tm_sec  = eft->second;
 	wtime->tm_min  = eft->minute;
 	wtime->tm_hour = eft->hour;
diff -Nur linux-2.4.22/drivers/char/ip27-rtc.c linux-2.4.22.rtc_fix/drivers/char/ip27-rtc.c
--- linux-2.4.22/drivers/char/ip27-rtc.c	2003-08-25 13:44:41.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/char/ip27-rtc.c	2004-02-14 05:04:59.000000000 +0100
@@ -83,6 +83,7 @@
 	switch (cmd) {
 	case RTC_RD_TIME:	/* Read the time/date from RTC	*/
 	{
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		get_rtc_time(&wtime);
 		break;
 	}
diff -Nur linux-2.4.22/drivers/char/mips_rtc.c linux-2.4.22.rtc_fix/drivers/char/mips_rtc.c
--- linux-2.4.22/drivers/char/mips_rtc.c	2003-08-25 13:44:41.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/char/mips_rtc.c	2004-02-14 05:05:39.000000000 +0100
@@ -82,6 +82,7 @@
 
 	switch (cmd) {
 	case RTC_RD_TIME:	/* Read the time/date from RTC  */
+		memset(&rtc_tm, 0, sizeof(struct rtc_time));
 		curr_time = rtc_get_time();
 		to_tm(curr_time, &rtc_tm);
 		rtc_tm.tm_year -= 1900;
diff -Nur linux-2.4.22/drivers/char/rtc.c linux-2.4.22.rtc_fix/drivers/char/rtc.c
--- linux-2.4.22/drivers/char/rtc.c	2003-06-13 16:51:33.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/char/rtc.c	2004-02-14 05:06:49.000000000 +0100
@@ -363,6 +363,7 @@
 		 * tm_min, and tm_sec values are filled in.
 		 */
 
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		get_rtc_alm_time(&wtime);
 		break; 
 	}
diff -Nur linux-2.4.22/drivers/hil/hp_sdc_rtc.c linux-2.4.22.rtc_fix/drivers/hil/hp_sdc_rtc.c
--- linux-2.4.22/drivers/hil/hp_sdc_rtc.c	2003-06-13 16:51:33.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/hil/hp_sdc_rtc.c	2004-02-14 05:08:34.000000000 +0100
@@ -561,6 +561,7 @@
         }
         case RTC_ALM_READ:      /* Read the present alarm time */
         {
+		memset(&ttime, 0, sizeof(struct timeval));
 		if (hp_sdc_rtc_read_mt(&ttime)) return -EFAULT;
                 break;
         }
@@ -609,6 +610,7 @@
         }
         case RTC_RD_TIME:       /* Read the time/date from RTC  */
         {
+		memset(&wtime, 0, sizeof(struct rtc_time));
 		if (hp_sdc_rtc_read_bbrtc(&wtime)) return -EFAULT;
                 break;
         }
diff -Nur linux-2.4.22/drivers/macintosh/rtc.c linux-2.4.22.rtc_fix/drivers/macintosh/rtc.c
--- linux-2.4.22/drivers/macintosh/rtc.c	2002-02-25 20:37:58.000000000 +0100
+++ linux-2.4.22.rtc_fix/drivers/macintosh/rtc.c	2004-02-14 05:09:26.000000000 +0100
@@ -64,6 +64,7 @@
 	case RTC_RD_TIME:
 		if (ppc_md.get_rtc_time)
 		{
+			memset(&rtc_tm, 0, sizeof(struct rtc_time));
 			get_rtc_time(&rtc_tm);
 
 			if (copy_to_user((struct rtc_time*)arg, &rtc_tm, sizeof(struct rtc_time)))
diff -Nur linux-2.4.22/drivers/sbus/char/rtc.c linux-2.4.22.rtc_fix/drivers/sbus/char/rtc.c
--- linux-2.4.22/drivers/sbus/char/rtc.c	2001-10-11 08:42:47.000000000 +0200
+++ linux-2.4.22.rtc_fix/drivers/sbus/char/rtc.c	2004-02-14 05:10:03.000000000 +0100
@@ -89,6 +89,7 @@
 	switch (cmd)
 	{
 	case RTCGET:
+		memset(&rtc_tm, 0, sizeof(struct rtc_time));
 		get_rtc_time(&rtc_tm);
 
 		if (copy_to_user((struct rtc_time*)arg, &rtc_tm, sizeof(struct rtc_time)))
