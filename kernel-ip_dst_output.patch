--- /usr/src/linux/include/net/ip.h.orig	2004-04-16 16:31:05.000000000 +0200
+++ /usr/src/linux/include/net/ip.h	2004-04-16 17:31:57.096851168 +0200
@@ -227,7 +227,7 @@
 		__ip_select_ident(iph, dst, more);
 }
 
-extern inline int ip_dst_output(struct sk_buff *skb)
+static inline int ip_dst_output(struct sk_buff *skb)
 {
 	return NF_HOOK_COND(PF_INET, NF_IP_POST_ROUTING, skb, NULL,
 	                    skb->dst->dev, dst_output, skb->dst->xfrm != NULL);
--- /usr/src/linux/net/ipv4/ip_output.c.orig	2004-04-16 16:31:10.000000000 +0200
+++ /usr/src/linux/net/ipv4/ip_output.c	2004-04-16 17:32:43.930731336 +0200
@@ -124,15 +124,6 @@
 	return ttl;
 }
 
-#ifdef CONFIG_NETFILTER
-/* out-of-line copy is only required with netfilter */
-int ip_dst_output(struct sk_buff *skb)
-{
-	return NF_HOOK_COND(PF_INET, NF_IP_POST_ROUTING, skb, NULL,
-	                    skb->dst->dev, dst_output, skb->dst->xfrm != NULL);
-}
-#endif
-
 /* 
  *		Add an ip header to a skbuff and send it out.
  *
