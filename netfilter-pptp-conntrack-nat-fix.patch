--- pptp-conntrack-nat.patch.org	Wed Nov 28 11:44:38 2001
+++ pptp-conntrack-nat.patch	Wed Jan 16 13:20:29 2002
@@ -409,12 +409,14 @@
  
  # Multipart objects.
  list-multi		:= ip_conntrack.o iptable_nat.o ipfwadm.o ipchains.o
-@@ -37,9 +37,11 @@
- 
+@@ -37,11 +37,13 @@
  # connection tracking helpers
  obj-$(CONFIG_IP_NF_FTP) += ip_conntrack_ftp.o
+ obj-$(CONFIG_IP_NF_EGG) += ip_conntrack_egg.o
 +obj-$(CONFIG_IP_NF_PPTP) += ip_conntrack_pptp.o
  
+ obj-$(CONFIG_IP_NF_CTNETLINK) += ip_conntrack_netlink.o
+ 
  # NAT helpers 
  obj-$(CONFIG_IP_NF_NAT_FTP) += ip_nat_ftp.o
 +obj-$(CONFIG_IP_NF_PPTP) += ip_nat_pptp.o
